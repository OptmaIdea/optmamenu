├── Dashboard
│   └── Painel operacional (layout-dashboard)
│   └── Atividades recentes (chart-no-axes-column)
│   └── Alertas (circle-alert)
│   └── Relatórios (file-stack)
├── Comercial
│   └── Pedidos (shopping-bag)
│   └── Canais de venda (radio-tower)
│   └── Pagamentos (wallet-cards)
│   └── Entregas (truck)
│   └── Dashboard comercial (chart-no-axes-column)
│   └── Configurações comerciais (settings)
│   └── Clientes (users)
│   └── Fidelidade (heart)
│   └── Fidelidade avançada (sparkles)
│   └── Mensagens (message-square)
│   └── Promoções (megaphone)
├── Financeiro
│   └── Livro diário (wallet-cards)
├── Produtos
│   └── Produtos (package)
│   └── Categorias (layers)
│   └── Estoque por local (file-text)
│   └── Vida do produto (activity)
│   └── Transferências (arrow-right-left)
│   └── Fornecedores (truck)
│   └── Compras (history)
│   └── Cotação (file-text)
│   └── Movimentação (history)
│   └── Configurações de Estoque (sliders-horizontal)
├── Configurações
│   └── Meus Dados (user-circle)
│   └── Pedido Online (smartphone)
│   └── Usuários (users)
│   └── Horários (clock)
│   └── Mensagens (message-circle)
│   └── Pagamento (credit-card)
│   └── Senhas e Acesso (shield)
└── Suporte
    └── Termos Legais (file-text)
    └── FAQ (help-circle)
    └── Documentação (book-open)