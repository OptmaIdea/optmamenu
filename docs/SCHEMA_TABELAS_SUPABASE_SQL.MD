-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.audit_logs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid,
  user_id uuid,
  action text NOT NULL,
  entity text,
  entity_id uuid,
  old_data jsonb,
  new_data jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT audit_logs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.categories (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  store_id uuid NOT NULL,
  name text NOT NULL,
  sort_order integer DEFAULT 0,
  active boolean DEFAULT true,
  price_logic_type text DEFAULT 'standard'::text,
  price_rules jsonb DEFAULT '[]'::jsonb,
  description text,
  image_url text,
  loyalty_eligible boolean DEFAULT true,
  loyalty_multiplier numeric DEFAULT 1.0,
  pricing_strategy jsonb,
  CONSTRAINT categories_pkey PRIMARY KEY (id),
  CONSTRAINT categories_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id)
);
CREATE TABLE public.customer_addresses (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  zip_code text NOT NULL,
  street text NOT NULL,
  number text NOT NULL,
  complement text,
  district text NOT NULL,
  city text NOT NULL,
  state text NOT NULL,
  is_default boolean DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT customer_addresses_pkey PRIMARY KEY (id),
  CONSTRAINT customer_addresses_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.customer_consent_logs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  consent_type text NOT NULL,
  action text NOT NULL,
  ip_address text,
  user_agent text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT customer_consent_logs_pkey PRIMARY KEY (id),
  CONSTRAINT customer_consent_logs_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.customer_notifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  store_id uuid NOT NULL,
  title text NOT NULL,
  message text NOT NULL,
  type text DEFAULT 'info'::text,
  read boolean DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT customer_notifications_pkey PRIMARY KEY (id),
  CONSTRAINT customer_notifications_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT customer_notifications_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id)
);
CREATE TABLE public.customer_otps (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  phone text NOT NULL,
  store_id uuid NOT NULL,
  otp_code text NOT NULL,
  expires_at timestamp with time zone NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  verified boolean DEFAULT false,
  otp_hash text,
  attempts integer NOT NULL DEFAULT 0,
  used boolean NOT NULL DEFAULT false,
  last_sent_at timestamp with time zone,
  CONSTRAINT customer_otps_pkey PRIMARY KEY (id)
);
CREATE TABLE public.customers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  phone text NOT NULL,
  full_name text,
  cpf text,
  email text,
  birth_date date,
  is_whatsapp boolean DEFAULT false,
  contact_preference text CHECK (contact_preference = ANY (ARRAY['whatsapp'::text, 'sms'::text, 'both'::text])),
  marketing_consent boolean DEFAULT false,
  loyalty_points integer DEFAULT 0,
  loyalty_tier text DEFAULT 'Bronze'::text CHECK (loyalty_tier = ANY (ARRAY['Bronze'::text, 'Prata'::text, 'Ouro'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  last_login timestamp with time zone,
  status text DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'inactive'::text, 'deleted_requested'::text])),
  nickname text,
  password_hash text,
  tags ARRAY DEFAULT '{}'::text[],
  loyalty_opt_in boolean DEFAULT false,
  current_stamps integer DEFAULT 0,
  current_tier_id uuid,
  last_point_activity_at timestamp with time zone DEFAULT now(),
  birth_date_locked boolean DEFAULT false,
  email_verified boolean DEFAULT false,
  CONSTRAINT customers_pkey PRIMARY KEY (id),
  CONSTRAINT customers_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT customers_current_tier_id_fkey FOREIGN KEY (current_tier_id) REFERENCES public.fidelity_tiers(id)
);
CREATE TABLE public.fidelity_programs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL UNIQUE,
  name text NOT NULL DEFAULT 'Programa de Fidelidade'::text,
  is_active boolean DEFAULT false,
  points_per_currency numeric DEFAULT 1.0,
  min_order_value numeric DEFAULT 0,
  enable_join_bonus boolean DEFAULT false,
  join_bonus_points integer DEFAULT 0,
  enable_birthday_bonus boolean DEFAULT false,
  birthday_bonus_points integer DEFAULT 0,
  enable_cashback boolean DEFAULT true,
  points_validity_months integer DEFAULT 12,
  min_points_redemption integer DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  enable_stamps boolean DEFAULT false,
  min_order_for_stamp numeric DEFAULT 0,
  stamps_target integer DEFAULT 10,
  points_per_stamp_block integer DEFAULT 0,
  warn_voucher_expiry_1 integer,
  warn_voucher_expiry_2 integer,
  warn_voucher_expiry_3 integer,
  program_terms text,
  voucher_terms text,
  CONSTRAINT fidelity_programs_pkey PRIMARY KEY (id),
  CONSTRAINT fidelity_programs_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id)
);
CREATE TABLE public.fidelity_rewards (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  program_id uuid NOT NULL,
  title text NOT NULL,
  description text,
  points_cost integer NOT NULL,
  type text NOT NULL CHECK (type = ANY (ARRAY['discount'::text, 'product'::text, 'free_shipping'::text, 'other'::text])),
  discount_amount numeric,
  max_redemptions_per_customer integer,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  stock_quantity integer,
  voucher_validity_days integer DEFAULT 15,
  image_url text,
  additional_cash_cost numeric DEFAULT 0.00,
  offer_valid_until timestamp with time zone,
  product_id uuid,
  product_quantity integer DEFAULT 1,
  discount_percentage numeric,
  max_discount_value numeric,
  min_order_value numeric,
  CONSTRAINT fidelity_rewards_pkey PRIMARY KEY (id),
  CONSTRAINT fidelity_rewards_program_id_fkey FOREIGN KEY (program_id) REFERENCES public.fidelity_programs(id),
  CONSTRAINT fidelity_rewards_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.fidelity_tiers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  name text NOT NULL,
  min_points integer NOT NULL DEFAULT 0,
  multiplier numeric NOT NULL DEFAULT 1.0,
  color text NOT NULL DEFAULT '#000000'::text,
  icon_url text,
  position integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT fidelity_tiers_pkey PRIMARY KEY (id),
  CONSTRAINT fidelity_tiers_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id)
);
CREATE TABLE public.fidelity_vouchers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  customer_id uuid NOT NULL,
  reward_id uuid,
  code text NOT NULL,
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'used'::text, 'expired'::text])),
  expires_at timestamp with time zone NOT NULL,
  used_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT fidelity_vouchers_pkey PRIMARY KEY (id),
  CONSTRAINT fidelity_vouchers_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT fidelity_vouchers_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT fidelity_vouchers_reward_id_fkey FOREIGN KEY (reward_id) REFERENCES public.fidelity_rewards(id)
);
CREATE TABLE public.inventory_balances (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  product_id uuid NOT NULL,
  variant_id uuid,
  on_hand numeric NOT NULL DEFAULT 0,
  reserved numeric NOT NULL DEFAULT 0,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT inventory_balances_pkey PRIMARY KEY (id),
  CONSTRAINT fk_inventory_balances_store FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT fk_inventory_balances_product FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.inventory_history_legacy (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  store_id uuid,
  product_id uuid,
  type USER-DEFINED NOT NULL,
  quantity integer NOT NULL,
  balance_before integer NOT NULL,
  balance_after integer NOT NULL,
  order_id uuid,
  user_id uuid,
  reason text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT inventory_history_legacy_pkey PRIMARY KEY (id),
  CONSTRAINT inventory_history_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT inventory_history_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT inventory_history_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id),
  CONSTRAINT inventory_history_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.inventory_location_balances (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  location_id uuid NOT NULL,
  product_id uuid NOT NULL,
  variant_id uuid,
  on_hand numeric NOT NULL DEFAULT 0,
  reserved numeric NOT NULL DEFAULT 0,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT inventory_location_balances_pkey PRIMARY KEY (id),
  CONSTRAINT inventory_location_balances_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT inventory_location_balances_location_id_fkey FOREIGN KEY (location_id) REFERENCES public.stock_locations(id),
  CONSTRAINT inventory_location_balances_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.inventory_movements (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  product_id uuid NOT NULL,
  variant_id uuid,
  type USER-DEFINED NOT NULL,
  quantity numeric NOT NULL CHECK (quantity > 0::numeric),
  reason text,
  reference text,
  order_id uuid,
  order_item_id uuid,
  created_by uuid,
  created_by_email text,
  meta jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT inventory_movements_pkey PRIMARY KEY (id),
  CONSTRAINT fk_inventory_movements_store FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT fk_inventory_movements_product FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.loyalty_transactions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  type text NOT NULL CHECK (type = ANY (ARRAY['order'::text, 'join_bonus'::text, 'birthday_bonus'::text, 'bonus'::text, 'redemption'::text, 'adjustment'::text, 'reversal'::text, 'expiry'::text])),
  points integer NOT NULL,
  description text,
  order_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  program_id uuid,
  related_transaction_id uuid,
  reward_id uuid,
  created_by uuid,
  CONSTRAINT loyalty_transactions_pkey PRIMARY KEY (id),
  CONSTRAINT loyalty_transactions_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT loyalty_transactions_program_id_fkey FOREIGN KEY (program_id) REFERENCES public.fidelity_programs(id),
  CONSTRAINT loyalty_transactions_related_transaction_id_fkey FOREIGN KEY (related_transaction_id) REFERENCES public.loyalty_transactions(id),
  CONSTRAINT loyalty_transactions_reward_id_fkey FOREIGN KEY (reward_id) REFERENCES public.fidelity_rewards(id),
  CONSTRAINT loyalty_transactions_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.loyalty_transactions_legacy (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  order_id uuid,
  amount integer NOT NULL,
  description text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT loyalty_transactions_legacy_pkey PRIMARY KEY (id),
  CONSTRAINT loyalty_transactions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.operational_timeline_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  entity_type text NOT NULL CHECK (entity_type = ANY (ARRAY['purchase_quotation'::text, 'purchase_document'::text, 'stock_transfer'::text, 'stock_movement'::text, 'supplier'::text, 'product'::text, 'order'::text, 'return'::text, 'customer'::text, 'system'::text, 'other'::text])),
  entity_id uuid NOT NULL,
  event_type text NOT NULL,
  title text NOT NULL,
  description text,
  severity text NOT NULL DEFAULT 'info'::text CHECK (severity = ANY (ARRAY['info'::text, 'success'::text, 'warning'::text, 'danger'::text, 'critical'::text])),
  status text NOT NULL DEFAULT 'done'::text CHECK (status = ANY (ARRAY['open'::text, 'done'::text, 'cancelled'::text, 'archived'::text])),
  actor_user_id uuid DEFAULT auth.uid(),
  actor_email text,
  responsible_name text,
  channel text DEFAULT 'system'::text CHECK (channel IS NULL OR (channel = ANY (ARRAY['system'::text, 'manual'::text, 'whatsapp'::text, 'email'::text, 'pdf'::text, 'phone'::text, 'in_person'::text, 'api'::text, 'import'::text, 'other'::text]))),
  occurred_at timestamp with time zone NOT NULL DEFAULT now(),
  source text NOT NULL DEFAULT 'system'::text,
  source_id uuid,
  old_data jsonb NOT NULL DEFAULT '{}'::jsonb,
  new_data jsonb NOT NULL DEFAULT '{}'::jsonb,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  related_supplier_id uuid,
  related_product_id uuid,
  related_purchase_quotation_id uuid,
  related_purchase_document_id uuid,
  related_stock_transfer_id uuid,
  related_stock_movement_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT operational_timeline_events_pkey PRIMARY KEY (id),
  CONSTRAINT operational_timeline_events_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT operational_timeline_events_actor_user_id_fkey FOREIGN KEY (actor_user_id) REFERENCES auth.users(id),
  CONSTRAINT operational_timeline_events_related_supplier_id_fkey FOREIGN KEY (related_supplier_id) REFERENCES public.suppliers(id),
  CONSTRAINT operational_timeline_events_related_product_id_fkey FOREIGN KEY (related_product_id) REFERENCES public.products(id),
  CONSTRAINT operational_timeline_events_related_purchase_quotation_id_fkey FOREIGN KEY (related_purchase_quotation_id) REFERENCES public.purchase_quotations(id),
  CONSTRAINT operational_timeline_events_related_purchase_document_id_fkey FOREIGN KEY (related_purchase_document_id) REFERENCES public.purchase_documents(id),
  CONSTRAINT operational_timeline_events_related_stock_transfer_id_fkey FOREIGN KEY (related_stock_transfer_id) REFERENCES public.stock_transfers(id),
  CONSTRAINT operational_timeline_events_related_stock_movement_id_fkey FOREIGN KEY (related_stock_movement_id) REFERENCES public.stock_movements(id)
);
CREATE TABLE public.order_items (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  order_id uuid,
  product_id uuid,
  quantity integer NOT NULL,
  unit_price numeric NOT NULL,
  discount numeric DEFAULT 0,
  CONSTRAINT order_items_pkey PRIMARY KEY (id),
  CONSTRAINT order_items_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id),
  CONSTRAINT order_items_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.orders (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid,
  store_id uuid,
  customer_name text,
  customer_phone text,
  status USER-DEFINED DEFAULT 'reserved'::order_status,
  total numeric NOT NULL DEFAULT 0,
  payment_method USER-DEFINED DEFAULT 'pending'::payment_method,
  proof_url text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  confirmed_at timestamp with time zone,
  completed_at timestamp with time zone,
  delivery_address text,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT orders_pkey PRIMARY KEY (id),
  CONSTRAINT orders_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT orders_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id)
);
CREATE TABLE public.otp_codes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  phone text NOT NULL,
  store_id uuid NOT NULL,
  code text NOT NULL,
  expires_at timestamp with time zone NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  attempts integer DEFAULT 0,
  used boolean DEFAULT false,
  CONSTRAINT otp_codes_pkey PRIMARY KEY (id),
  CONSTRAINT otp_codes_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id)
);
CREATE TABLE public.products (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  store_id uuid NOT NULL,
  category_id uuid,
  name text NOT NULL,
  description text,
  price numeric NOT NULL,
  active boolean DEFAULT true,
  sort_order integer DEFAULT 0,
  use_category_pricing boolean DEFAULT false,
  price_logic_type text DEFAULT 'standard'::text,
  price_rules jsonb DEFAULT '[]'::jsonb,
  stock_quantity integer DEFAULT 0,
  images ARRAY DEFAULT ARRAY[]::text[],
  min_stock integer DEFAULT 5,
  max_stock integer DEFAULT 20,
  last_sale_at timestamp with time zone,
  last_stock_entry_at timestamp with time zone,
  discontinued boolean DEFAULT false,
  is_discontinued boolean DEFAULT false,
  last_entry_unit_cost numeric,
  CONSTRAINT products_pkey PRIMARY KEY (id),
  CONSTRAINT products_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT products_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.categories(id)
);
CREATE TABLE public.profiles (
  id uuid NOT NULL,
  phone text UNIQUE,
  name text,
  address text,
  district text,
  city text DEFAULT 'São João Nepomuceno'::text,
  birthdata date,
  cpf text,
  points integer DEFAULT 0,
  accepted_policy boolean DEFAULT false,
  accepted_promo boolean DEFAULT false,
  is_admin boolean DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  is_active boolean DEFAULT true,
  internal_notes text,
  loyalty_points integer DEFAULT 0,
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.promotions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name text NOT NULL,
  description text,
  type USER-DEFINED NOT NULL,
  value numeric NOT NULL,
  category_id uuid,
  product_id uuid,
  start_date timestamp with time zone,
  end_date timestamp with time zone,
  active boolean DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT promotions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.purchase_document_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  purchase_document_id uuid NOT NULL,
  product_id uuid NOT NULL,
  quantity numeric NOT NULL,
  unit_cost numeric NOT NULL,
  total_cost numeric DEFAULT (quantity * unit_cost),
  store_id uuid NOT NULL,
  CONSTRAINT purchase_document_items_pkey PRIMARY KEY (id),
  CONSTRAINT purchase_document_items_document_id_fkey FOREIGN KEY (purchase_document_id) REFERENCES public.purchase_documents(id),
  CONSTRAINT purchase_document_items_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT purchase_document_items_purchase_document_id_fkey FOREIGN KEY (purchase_document_id) REFERENCES public.purchase_documents(id)
);
CREATE TABLE public.purchase_document_location_applications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  purchase_document_id uuid NOT NULL,
  purchase_document_item_id uuid NOT NULL UNIQUE,
  store_id uuid NOT NULL,
  supplier_id uuid NOT NULL,
  location_id uuid NOT NULL,
  product_id uuid NOT NULL,
  quantity numeric NOT NULL CHECK (quantity > 0::numeric),
  unit_cost numeric NOT NULL DEFAULT 0,
  applied_at timestamp with time zone NOT NULL DEFAULT now(),
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT purchase_document_location_applications_pkey PRIMARY KEY (id),
  CONSTRAINT purchase_document_location_applications_document_fkey FOREIGN KEY (purchase_document_id) REFERENCES public.purchase_documents(id),
  CONSTRAINT purchase_document_location_applications_item_fkey FOREIGN KEY (purchase_document_item_id) REFERENCES public.purchase_document_items(id),
  CONSTRAINT purchase_document_location_applications_store_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT purchase_document_location_applications_supplier_fkey FOREIGN KEY (supplier_id) REFERENCES public.suppliers(id),
  CONSTRAINT purchase_document_location_applications_location_fkey FOREIGN KEY (location_id) REFERENCES public.stock_locations(id),
  CONSTRAINT purchase_document_location_applications_product_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.purchase_documents (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  store_id uuid NOT NULL,
  supplier_id uuid NOT NULL,
  invoice_number text,
  issue_date date DEFAULT CURRENT_DATE,
  total_amount numeric DEFAULT 0,
  notes text,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'confirmed'::text, 'cancelled'::text])),
  cancelled_at timestamp with time zone,
  cancelled_by uuid,
  cancel_reason text,
  document_code text,
  CONSTRAINT purchase_documents_pkey PRIMARY KEY (id),
  CONSTRAINT purchase_documents_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT purchase_documents_supplier_id_fkey FOREIGN KEY (supplier_id) REFERENCES public.suppliers(id),
  CONSTRAINT purchase_documents_cancelled_by_fkey FOREIGN KEY (cancelled_by) REFERENCES auth.users(id)
);
CREATE TABLE public.purchase_quotation_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  quotation_id uuid NOT NULL,
  store_id uuid NOT NULL,
  product_id uuid NOT NULL,
  requested_qty numeric NOT NULL CHECK (requested_qty > 0::numeric),
  reference_unit_cost numeric CHECK (reference_unit_cost IS NULL OR reference_unit_cost >= 0::numeric),
  quoted_unit_cost numeric CHECK (quoted_unit_cost IS NULL OR quoted_unit_cost >= 0::numeric),
  approved_qty numeric CHECK (approved_qty IS NULL OR approved_qty >= 0::numeric),
  notes text,
  supplier_notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT purchase_quotation_items_pkey PRIMARY KEY (id),
  CONSTRAINT purchase_quotation_items_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT purchase_quotation_items_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT purchase_quotation_items_quotation_id_fkey FOREIGN KEY (quotation_id) REFERENCES public.purchase_quotations(id)
);
CREATE TABLE public.purchase_quotations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  supplier_id uuid NOT NULL,
  quotation_code text NOT NULL,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'sent'::text, 'answered'::text, 'approved'::text, 'rejected'::text, 'converted'::text, 'cancelled'::text])),
  requested_at timestamp with time zone NOT NULL DEFAULT now(),
  responded_at timestamp with time zone,
  expires_at timestamp with time zone,
  sent_channel text CHECK (sent_channel IS NULL OR (sent_channel = ANY (ARRAY['whatsapp'::text, 'email'::text, 'pdf'::text, 'manual'::text, 'other'::text]))),
  responsible_name text,
  message_subject text,
  message_body text,
  notes text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_by uuid DEFAULT auth.uid(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  converted_purchase_document_id uuid,
  CONSTRAINT purchase_quotations_pkey PRIMARY KEY (id),
  CONSTRAINT purchase_quotations_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT purchase_quotations_supplier_id_fkey FOREIGN KEY (supplier_id) REFERENCES public.suppliers(id),
  CONSTRAINT purchase_quotations_converted_purchase_document_id_fkey FOREIGN KEY (converted_purchase_document_id) REFERENCES public.purchase_documents(id)
);
CREATE TABLE public.stock_locations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  code text NOT NULL,
  name text NOT NULL,
  type USER-DEFINED NOT NULL DEFAULT 'main'::stock_location_type,
  description text,
  active boolean NOT NULL DEFAULT true,
  is_default boolean NOT NULL DEFAULT false,
  allow_sales boolean NOT NULL DEFAULT false,
  allow_reservations boolean NOT NULL DEFAULT false,
  sort_order integer NOT NULL DEFAULT 0,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stock_locations_pkey PRIMARY KEY (id),
  CONSTRAINT stock_locations_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id)
);
CREATE TABLE public.stock_movements (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid NOT NULL,
  order_id uuid,
  quantity integer NOT NULL,
  type USER-DEFINED NOT NULL,
  reason text,
  user_id uuid,
  previous_stock integer NOT NULL DEFAULT 0,
  new_stock integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  store_id uuid NOT NULL,
  affects_physical boolean NOT NULL DEFAULT true,
  source text,
  source_id uuid,
  reason_code text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_by uuid,
  supplier_id uuid,
  location_id uuid,
  from_location_id uuid,
  to_location_id uuid,
  transfer_id uuid,
  CONSTRAINT stock_movements_pkey PRIMARY KEY (id),
  CONSTRAINT stock_movements_supplier_id_fkey FOREIGN KEY (supplier_id) REFERENCES public.suppliers(id),
  CONSTRAINT stock_movements_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT stock_movements_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id),
  CONSTRAINT stock_movements_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT stock_movements_location_id_fkey FOREIGN KEY (location_id) REFERENCES public.stock_locations(id),
  CONSTRAINT stock_movements_from_location_id_fkey FOREIGN KEY (from_location_id) REFERENCES public.stock_locations(id),
  CONSTRAINT stock_movements_to_location_id_fkey FOREIGN KEY (to_location_id) REFERENCES public.stock_locations(id),
  CONSTRAINT stock_movements_transfer_id_fkey FOREIGN KEY (transfer_id) REFERENCES public.stock_transfers(id),
  CONSTRAINT stock_movements_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id)
);
CREATE TABLE public.stock_reservations (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  order_id uuid,
  product_id uuid,
  quantity integer NOT NULL,
  expires_at timestamp with time zone NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  store_id uuid NOT NULL,
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'consumed'::text, 'cancelled'::text, 'expired'::text])),
  consumed_at timestamp with time zone,
  cancelled_at timestamp with time zone,
  CONSTRAINT stock_reservations_pkey PRIMARY KEY (id),
  CONSTRAINT stock_reservations_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT stock_reservations_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id)
);
CREATE TABLE public.stock_transfer_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  transfer_id uuid NOT NULL,
  store_id uuid NOT NULL,
  product_id uuid NOT NULL,
  variant_id uuid,
  requested_qty numeric NOT NULL DEFAULT 0 CHECK (requested_qty >= 0::numeric),
  shipped_qty numeric NOT NULL DEFAULT 0 CHECK (shipped_qty >= 0::numeric),
  received_qty numeric NOT NULL DEFAULT 0 CHECK (received_qty >= 0::numeric),
  divergence_qty numeric NOT NULL DEFAULT 0 CHECK (divergence_qty >= 0::numeric),
  unit_cost numeric,
  notes text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  divergence_resolution text CHECK (divergence_resolution IS NULL OR (divergence_resolution = ANY (ARRAY['loss'::text, 'return_to_origin'::text, 'accepted_shortage'::text]))),
  divergence_reason text,
  divergence_notes text,
  loss_qty numeric NOT NULL DEFAULT 0 CHECK (loss_qty >= 0::numeric),
  returned_to_origin_qty numeric NOT NULL DEFAULT 0 CHECK (returned_to_origin_qty >= 0::numeric),
  accepted_shortage_qty numeric NOT NULL DEFAULT 0 CHECK (accepted_shortage_qty >= 0::numeric),
  divergence_resolved_at timestamp with time zone,
  CONSTRAINT stock_transfer_items_pkey PRIMARY KEY (id),
  CONSTRAINT stock_transfer_items_transfer_id_fkey FOREIGN KEY (transfer_id) REFERENCES public.stock_transfers(id),
  CONSTRAINT stock_transfer_items_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT stock_transfer_items_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.stock_transfers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  source_store_id uuid NOT NULL,
  destination_store_id uuid NOT NULL,
  source_location_id uuid NOT NULL,
  destination_location_id uuid NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::stock_transfer_status,
  transfer_code text,
  requested_by uuid,
  approved_by uuid,
  shipped_by uuid,
  received_by uuid,
  requested_at timestamp with time zone NOT NULL DEFAULT now(),
  approved_at timestamp with time zone,
  shipped_at timestamp with time zone,
  received_at timestamp with time zone,
  cancelled_at timestamp with time zone,
  notes text,
  cancel_reason text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stock_transfers_pkey PRIMARY KEY (id),
  CONSTRAINT stock_transfers_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT stock_transfers_source_store_id_fkey FOREIGN KEY (source_store_id) REFERENCES public.stores(id),
  CONSTRAINT stock_transfers_destination_store_id_fkey FOREIGN KEY (destination_store_id) REFERENCES public.stores(id),
  CONSTRAINT stock_transfers_source_location_id_fkey FOREIGN KEY (source_location_id) REFERENCES public.stock_locations(id),
  CONSTRAINT stock_transfers_destination_location_id_fkey FOREIGN KEY (destination_location_id) REFERENCES public.stock_locations(id)
);
CREATE TABLE public.store_hours (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid,
  day_of_week integer NOT NULL CHECK (day_of_week >= 0 AND day_of_week <= 6),
  open_time time without time zone DEFAULT '09:00:00'::time without time zone,
  close_time time without time zone DEFAULT '18:00:00'::time without time zone,
  is_closed boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT store_hours_pkey PRIMARY KEY (id),
  CONSTRAINT store_hours_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id)
);
CREATE TABLE public.store_messages (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  title text NOT NULL,
  message text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  expires_at timestamp with time zone,
  CONSTRAINT store_messages_pkey PRIMARY KEY (id),
  CONSTRAINT store_messages_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id)
);
CREATE TABLE public.store_schedules_exceptions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid,
  exception_date date NOT NULL,
  is_closed boolean DEFAULT true,
  open_time time without time zone,
  close_time time without time zone,
  reason text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT store_schedules_exceptions_pkey PRIMARY KEY (id),
  CONSTRAINT store_schedules_exceptions_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id)
);
CREATE TABLE public.store_security_logs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid,
  user_id uuid,
  user_email text,
  action text NOT NULL,
  details jsonb DEFAULT '{}'::jsonb,
  outcome text CHECK (outcome = ANY (ARRAY['success'::text, 'failure'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT store_security_logs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.stores (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  user_id uuid NOT NULL,
  slug text NOT NULL UNIQUE CHECK (slug ~ '^[a-z0-9]+(?:-[a-z0-9]+)*$'::text),
  name text NOT NULL,
  description text,
  logo_url text,
  phone_number text,
  theme_config jsonb DEFAULT '{}'::jsonb,
  legal_name text,
  doc_type text CHECK (doc_type = ANY (ARRAY['PF'::text, 'PJ'::text])),
  document text,
  fantasy_name text,
  establishment_type text,
  address jsonb DEFAULT '{}'::jsonb,
  contacts jsonb DEFAULT '{}'::jsonb,
  consents jsonb DEFAULT '{}'::jsonb,
  sms_gateway_token text,
  reservation_time_minutes integer DEFAULT 10,
  stock_password_hash text,
  config jsonb DEFAULT '{"visual_title": "", "visual_icon_url": "", "extension_minutes": 10, "visual_color_primary": "#00D65F", "timer_duration_minutes": 10, "visual_color_secondary": "#f9fafb"}'::jsonb,
  privacy_policy_text text,
  terms_of_use_text text,
  cookie_policy_text text,
  dpo_email text,
  dpo_contact text,
  token_expiry_seconds integer DEFAULT 45 CHECK (token_expiry_seconds >= 15 AND token_expiry_seconds <= 60),
  max_token_attempts integer DEFAULT 3 CHECK (max_token_attempts >= 3 AND max_token_attempts <= 7),
  CONSTRAINT stores_pkey PRIMARY KEY (id),
  CONSTRAINT stores_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.supplier_contacts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  supplier_id uuid NOT NULL,
  name text NOT NULL,
  role text,
  department text NOT NULL DEFAULT 'commercial'::text CHECK (department = ANY (ARRAY['commercial'::text, 'financial'::text, 'fiscal'::text, 'logistics'::text, 'support'::text, 'other'::text])),
  phone text,
  whatsapp text,
  email text,
  notes text,
  is_primary boolean NOT NULL DEFAULT false,
  active boolean NOT NULL DEFAULT true,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT supplier_contacts_pkey PRIMARY KEY (id),
  CONSTRAINT supplier_contacts_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT supplier_contacts_supplier_id_fkey FOREIGN KEY (supplier_id) REFERENCES public.suppliers(id)
);
CREATE TABLE public.supplier_price_history (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  supplier_id uuid NOT NULL,
  product_id uuid NOT NULL,
  purchase_document_id uuid,
  purchase_document_item_id uuid,
  unit_cost numeric NOT NULL CHECK (unit_cost >= 0::numeric),
  quantity numeric NOT NULL CHECK (quantity > 0::numeric),
  total_cost numeric DEFAULT (quantity * unit_cost),
  issue_date date,
  effective_at timestamp with time zone NOT NULL DEFAULT now(),
  source text NOT NULL DEFAULT 'purchase_document'::text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  is_active boolean NOT NULL DEFAULT true,
  cancelled_at timestamp with time zone,
  cancelled_reason text,
  CONSTRAINT supplier_price_history_pkey PRIMARY KEY (id),
  CONSTRAINT supplier_price_history_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT supplier_price_history_supplier_id_fkey FOREIGN KEY (supplier_id) REFERENCES public.suppliers(id),
  CONSTRAINT supplier_price_history_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT supplier_price_history_purchase_document_id_fkey FOREIGN KEY (purchase_document_id) REFERENCES public.purchase_documents(id),
  CONSTRAINT supplier_price_history_purchase_document_item_id_fkey FOREIGN KEY (purchase_document_item_id) REFERENCES public.purchase_document_items(id)
);
CREATE TABLE public.supplier_relationship_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  store_id uuid NOT NULL,
  supplier_id uuid NOT NULL,
  event_type text NOT NULL DEFAULT 'note'::text CHECK (event_type = ANY (ARRAY['note'::text, 'call'::text, 'meeting'::text, 'email'::text, 'incident'::text, 'purchase'::text, 'approval'::text, 'rejection'::text, 'block'::text, 'unblock'::text, 'status_change'::text])),
  title text NOT NULL,
  description text,
  event_at timestamp with time zone NOT NULL DEFAULT now(),
  severity text NOT NULL DEFAULT 'info'::text CHECK (severity = ANY (ARRAY['info'::text, 'low'::text, 'medium'::text, 'high'::text, 'critical'::text])),
  status text NOT NULL DEFAULT 'open'::text CHECK (status = ANY (ARRAY['open'::text, 'done'::text, 'archived'::text, 'cancelled'::text])),
  related_purchase_document_id uuid,
  related_product_id uuid,
  created_by uuid,
  created_by_email text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT supplier_relationship_events_pkey PRIMARY KEY (id),
  CONSTRAINT supplier_relationship_events_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id),
  CONSTRAINT supplier_relationship_events_supplier_id_fkey FOREIGN KEY (supplier_id) REFERENCES public.suppliers(id),
  CONSTRAINT supplier_relationship_events_purchase_document_fkey FOREIGN KEY (related_purchase_document_id) REFERENCES public.purchase_documents(id),
  CONSTRAINT supplier_relationship_events_product_fkey FOREIGN KEY (related_product_id) REFERENCES public.products(id),
  CONSTRAINT supplier_relationship_events_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.suppliers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone,
  store_id uuid NOT NULL,
  name text NOT NULL,
  document text,
  phone text,
  email text,
  notes text,
  active boolean NOT NULL DEFAULT true,
  legal_name text,
  trade_name text,
  person_type text CHECK (person_type IS NULL OR (person_type = ANY (ARRAY['PF'::text, 'PJ'::text, 'OUTRO'::text]))),
  state_registration text,
  municipal_registration text,
  tax_regime text,
  cnae_code text,
  icms_taxpayer_indicator text,
  fiscal_notes text,
  commercial_contact_name text,
  commercial_contact_role text,
  commercial_phone text,
  commercial_whatsapp text,
  commercial_email text,
  financial_contact_name text,
  financial_phone text,
  financial_email text,
  fiscal_contact_name text,
  fiscal_phone text,
  fiscal_email text,
  secondary_phone text,
  website text,
  service_hours jsonb NOT NULL DEFAULT '{}'::jsonb,
  address jsonb NOT NULL DEFAULT '{}'::jsonb,
  billing_address jsonb NOT NULL DEFAULT '{}'::jsonb,
  shipping_address jsonb NOT NULL DEFAULT '{}'::jsonb,
  payment_terms text,
  average_payment_days integer CHECK (average_payment_days IS NULL OR average_payment_days >= 0),
  credit_limit numeric NOT NULL DEFAULT 0 CHECK (credit_limit >= 0::numeric),
  bank_info jsonb NOT NULL DEFAULT '{}'::jsonb,
  pix_key text,
  pix_key_type text,
  beneficiary_name text,
  minimum_order_value numeric NOT NULL DEFAULT 0 CHECK (minimum_order_value >= 0::numeric),
  freight_policy text,
  delivery_days integer CHECK (delivery_days IS NULL OR delivery_days >= 0),
  lead_time_days integer CHECK (lead_time_days IS NULL OR lead_time_days >= 0),
  purchase_frequency text,
  commercial_terms text,
  homologation_status text NOT NULL DEFAULT 'not_evaluated'::text CHECK (homologation_status = ANY (ARRAY['not_evaluated'::text, 'approved'::text, 'pending'::text, 'rejected'::text, 'blocked'::text])),
  preferred_supplier boolean NOT NULL DEFAULT false,
  blocked boolean NOT NULL DEFAULT false,
  blocked_reason text,
  blocked_at timestamp with time zone,
  last_contact_at timestamp with time zone,
  relationship_notes text,
  tags ARRAY NOT NULL DEFAULT '{}'::text[],
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT suppliers_pkey PRIMARY KEY (id),
  CONSTRAINT suppliers_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.stores(id)
);
CREATE TABLE public.user_pins (
  user_id uuid NOT NULL,
  pin_hash text NOT NULL,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT user_pins_pkey PRIMARY KEY (user_id),
  CONSTRAINT user_pins_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);